# Bernstein-Vazirani Math

After phase kickback:

```text
1/sqrt(2^n) sum_x (-1)^(s dot x) |x>
```

The Hadamard transform identity:

```text
H^n |s> = 1/sqrt(2^n) sum_x (-1)^(s dot x)|x>
```

means applying `H^n` returns `|s>`.

