# Entanglement Math

The Bell state:

```text
|Phi+> = (|00> + |11>) / sqrt(2)
```

cannot be decomposed as:

```text
(a|0> + b|1>) tensor (c|0> + d|1>)
```

because that product would generally include `|01>` and `|10>` terms unless constraints force one of the desired terms to vanish.

