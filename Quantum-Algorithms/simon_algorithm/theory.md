# Simon Theory

The oracle collapses the input register to a pair `|x> + |x xor s>`. Hadamards transform this pair into samples `z` orthogonal to `s`. Enough samples reveal `s` through linear algebra over binary fields.

