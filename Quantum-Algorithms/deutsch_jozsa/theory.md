# Deutsch-Jozsa Theory

The output qubit in `|->` converts the oracle value into a phase. Constant functions apply a global phase, preserving constructive interference at `|0...0>`. Balanced functions cancel the all-zero amplitude.

