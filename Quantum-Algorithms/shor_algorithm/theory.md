# Shor Theory

Factoring can be solved if we can find the order `r` of a number `a` modulo `N`, where `a^r = 1 mod N`. Quantum period finding creates a periodic state whose frequency encodes `r`. The QFT converts periodic spacing in the computational basis into peaks in the frequency basis, making `r` recoverable through continued fractions.

